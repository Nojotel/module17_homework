import {revertString} from './revertStr.js';

describe('Add -cтрока get a reverse -акортc', () => {
  it('add -строка get a reverse -акортс', () => {
    expect(revertString('cтрока')).toBe('акортc');
    });
});